# DIAGNOSTIC ONLY — delete after use.
#
# Purpose: determine whether GitHub Actions can start at all for this account.
# The private repository kgarner310/idea-atlas produces conclusion=startup_failure
# with zero jobs on every push, across two repository names and two workflow
# files. Actions is confirmed enabled there.
#
# This repository is PUBLIC, and public repositories do not consume the
# account's included Actions minutes. So:
#   * runs here  -> Actions works; the private repo's failure points at minutes
#   * fails here -> the block is account-wide and minutes are the wrong lead
#
# Scoped to claude/** branches so it never touches main.
name: Actions probe

on:
  push:
    branches: ['claude/**']

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions can start for this account"
