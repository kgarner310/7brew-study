"""Ingestion: retrieve, validate, normalize, hash, and version official legal text.

Everything retrieved from the network is untrusted input. It is never executed,
never interpolated into a shell, and never treated as instructions to a model.
See docs/SECURITY.md and docs/THREAT_MODEL.md.
"""
